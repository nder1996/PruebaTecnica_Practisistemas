/** 
 * Archivo de entorno para configuraciones de la aplicación en el entorno de producción.
 * En este archivo, la configuración de producción indica que la aplicación está en modo de producción.
 */
export const environment = {
  production: true
};